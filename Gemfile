source 'https://rubygems.org'

ruby file: '.ruby-version'

gem 'sinatra' # For the web server
gem 'sass-embedded' # For compiling SCSS using the embedded Sass
gem 'bootstrap', '5.3.3' # Just this version for now

group :development do
  gem 'rerun' # For auto-reloading the server
  gem 'kamal'
end

gem "rackup", "~> 2.1"
